#include<stdio.h>
#include<iostream>

using namespace std;

void reverseWords(string &s) {
        int len = s.length();
        for( int i=0; i<len/2; i++ ) {
            s[i] = s[len-1-i];
        }
        cout << s << "\n";
    }
}
